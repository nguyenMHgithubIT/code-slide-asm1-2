class Node {
    Student data;   // Data of the node
    Node next;     // Pointer to the next node

    public Node(Student data) {
        this.data = data;
        this.next = null;
    }
}
